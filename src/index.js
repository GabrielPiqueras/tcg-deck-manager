import React from 'react';
import ReactDOM from 'react-dom/client';
import PokeCheck from './PokeCheck';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<PokeCheck />);